extends layout

block content
  include navbar.pug
  .w3-container
    h1.w3-center Upload Resource
    ul.w3-ul.w3-center
      b Warning
      li.w3-center.w3-justify The resource must be in ZIP format.
      li.w3-center.w3-justify Make sure that the metadata file in JSON format exists and that it is valid.
      li.w3-center.w3-justify Make sure that the manifest file (RRD-SIP) in JSON format exists and that it is valid.
  .w3-container.w3-center(style="display:flex; width= 80%")
    form.w3-container.w3-auto(action="/resources/upload" method="post" enctype="multipart/form-data")
      .w3-cell-row
        label.w3-text-light-green
          b Select File
        input.w3-input.w3-border.w3-light-grey(type="file" name="myFile")
      .w3-cell-row.w3-center
        button.w3-btn.w3-green(type="submit" value="Submit") Submit